<div class="well-done-wrapper">
    <div class="well-done-card">
        <div class="icon-box">
            <i class="fas fa-star"></i>
        </div>
        <h1>Well Done!</h1>
        <p>Thank you for sharing your experience with us.</p>
        <p class="sub-text">We are so glad you enjoyed your stay at <strong>D'Calvins Luxury Apartment</strong>. Your feedback helps us maintain our gold standard of service.</p>
        
        <div class="action-btns">
            <a href="/" class="btn-purple">Return Home</a>
        </div>
    </div>
</div>

<style>
    .well-done-wrapper {
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 70vh;
        padding: 40px 20px;
        background-color: #f9f9f9;
    }
    .well-done-card {
        background: white;
        padding: 50px 30px;
        border-radius: 20px;
        box-shadow: 0 15px 40px rgba(0,0,0,0.08);
        text-align: center;
        max-width: 550px;
        width: 100%;
    }
    .icon-box {
        width: 80px;
        height: 80px;
        background: var(--accent-yellow);
        color: var(--primary-purple);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        margin: 0 auto 25px;
        font-size: 2.5rem;
    }
    .well-done-card h1 {
        color: var(--primary-purple);
        font-size: 2.5rem;
        margin-bottom: 15px;
    }
    .sub-text {
        color: #777;
        line-height: 1.8;
        margin: 20px 0 35px;
    }
    .action-btns {
        display: flex;
    justify-content: center; /* Centers horizontally on desktop */
    align-items: center;     /* Aligns them perfectly in the middle */
    gap: 20px;               /* Space between the two buttons */
    margin-top: 30px;
    width: 100%;             /* Ensures it takes the full width of the card */
    flex-wrap: wrap;         /* Allows them to stack if the screen is tiny */
    }
    .btn-outline-dark {
        border: 2px solid var(--dark-black);
        color: var(--dark-black);
        padding: 12px 25px;
        text-decoration: none;
        border-radius: 8px;
        font-weight: bold;
        transition: 0.3s;
    }
    .btn-outline-dark:hover {
        background: var(--dark-black);
        color: white;
    }

    /* Mobile Fix */
    @media (max-width: 600px) {
        .well-done-card { padding: 30px 20px; }
        .action-btns { flex-direction: column; }
        .btn-purple, .btn-outline-dark { width: 100%; }
    }
</style>